App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Clear(coladdline);
        Reset(txtCustomerSearch_1);
        Reset(serialnumsearch);
        //Set(varDisableButton, true);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

